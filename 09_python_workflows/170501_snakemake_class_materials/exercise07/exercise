###
### Exercise 07
###

# load the singularity module
module load singularity

# execute the container directly
#   - uses the runscript packaged into the container
00scripts/rnaseq.img
00scripts/rnaseq.img list

# using the wrapper script
./rnaseq samtools
